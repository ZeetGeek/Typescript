const isLoggedIn: boolean = true;
console.log(isLoggedIn);

const isEven = (val: number): boolean => {
    return val % 4 === 0 && val % 8 === 0 ? true : false;
};

console.log(isEven(2));

// big Int

const bigNum: bigint = 912832156315236512341425125n;

console.log(bigNum);

const age: number = 30;
