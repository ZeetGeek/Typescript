"use strict";
const sum = (a, b) => a + b;
console.log(sum(20, 15));
// ==== run typescript file ==== //
/** write [tsc] and [tsc plus file name] like this [tsc index.ts] */
// ==== tsconfig.json ==== //
/** to create tsconfig.json file write command called [ tsc --init ] */
